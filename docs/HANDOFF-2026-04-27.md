# Handoff — 2026-04-27 lease e-signatures

**Repo HEAD:** `3854d08` on `main` · **Repo state:** clean
**Migration applied:** `2026_04_27_lease_signatures.sql` (live in Supabase)

Single commit on top of yesterday's mega-sprint. Adds e-signatures
to leases without external accounts.

---

## What shipped

- `lease_signatures` table — party (tenant/landlord) + status
  (pending/signed/voided) + sign_token + typed_name +
  signature_image_path + signed_at + signed_ip + signed_user_agent
  + voided_at/reason
- `lease-signatures` private storage bucket (signed URLs only)
- Token-authenticated public sign flow at `/lease-sign/[token]`
  (no login; 32-byte base64url; 14-day TTL; single-use)
- Server actions: `requestTenantSignature`, `voidPendingSignature`,
  `signLeaseAsLandlord`, `recordTenantSignature`
- `SignaturePad` component — HTML5 canvas, high-DPI scaled, mouse
  + touch + stylus, exports PNG dataUrl
- `LeaseSignaturePanel` on lease detail — side-by-side parties,
  status badges, IP/timestamp on signed rows, send-link button +
  landlord-sign modal
- `leases.signed_at` auto-stamped when both parties have signed

---

## Demo path

1. Tenants → click any tenant → click a lease
2. Scroll to "E-signatures" panel
3. Click **Send for tenant signature** → URL appears in tenant column
4. Open the URL in incognito → tenant signing page (no login)
5. Type name, draw signature, **Sign lease** → status → "Signed"
6. Back in dashboard, click **Sign as landlord** → modal → sign
7. Both parties signed → "Fully executed" badge appears,
   `leases.signed_at` populated

---

## Env var to set on Vercel

| Var | Used by |
|---|---|
| `NEXT_PUBLIC_APP_URL` | Lease-sign link generation (shown in dashboard for landlord to copy) |

Existing vars from yesterday still apply (`CRON_SECRET`,
`SUPABASE_SERVICE_ROLE_KEY`, optional `ANTHROPIC_API_KEY`).

---

## What's left

**Me-only, substantial:**
1. Listing AI copy generator (template stub → live with `ANTHROPIC_API_KEY`)
2. Stamp signature images into the lease PDF download (currently
   the table holds them; PDF doesn't embed yet)

**Me-only, smaller:**
- Signature audit-log table separate from the row itself
- Question audit (FairScreen) — needs application-question editor first
- Standalone findings inbox page
- Email-template customization

**Needs Arthur's external setup:**
- Stripe rent collection
- Live LLM activation
- Twilio Lookup, Checkr/Persona
- Attorney review of FairScreen rule packs

---

## Reference docs

- `docs/HANDOFF-2026-04-24.md` — Sprint 13/14 (historical)
- `docs/HANDOFF-2026-04-26.md` — mega-build sprint
- `docs/HANDOFF-2026-04-27.md` — this doc (lease e-sign)
- `docs/PROOF-CHECK-SPEC.md` + `docs/SPRINT-PROOF-CHECK-NEEDS.md`
- `docs/FAIRSCREEN-SPEC.md` + `docs/HANDOFF-FAIRSCREEN.md` + `docs/SPRINT-FAIRSCREEN-NEEDS.md`

---

## Critical rules (carry-forward)

1. units use `unit_number`; listings use `headline_rent`
2. Proof Check decision bar: all 3 buttons stay enabled regardless of risk band
3. DI engine NEVER reads name/email/phone/address
4. All FairScreen rule packs ATTORNEY-REVIEW-PENDING
5. Lease-sign tokens single-use, 14-day TTL, nulled after success
