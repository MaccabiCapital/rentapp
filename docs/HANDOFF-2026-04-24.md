# Rentapp / Rentbase — Session Handoff (2026-04-24)

**Session span:** 2026-04-22 → 2026-04-24
**Branch:** `main`
**Remote:** `MaccabiCapital/rentapp` (GitHub)
**Last commit:** `bad6dac — feat: add 'assigned' maintenance status`
**Uncommitted changes:** 97 files (14 modified, 83 new) — see *Git state* section
**tsc:** clean
**eslint:** clean

---

## TL;DR

Three days of work shipped a massive layer on top of the Sprint 13 baseline:
7 new feature modules (inspections, notices, renters insurance, leasing assistant,
reports, tenant portal, guided workflows), 3 CSV imports, a fair-housing audit
trail, syndication feed to Zillow, and a public application form — all on top of
a reorganized sidebar (Units / Tenants / Operations). Everything is stubbed
behind feature flags where an external account is needed, so activation is a
one-file swap per integration.

No commit has been made yet. All 97 changed files are uncommitted on `main`.

---

## Environment status

### Supabase project
- **ID:** `tbwzsdlgoyqlmsfajgdd` ("rent")
- **Region:** `us-east-1`
- **Postgres:** 17.6.1.104
- **Migrations applied this session (in order):**
  1. `2026_04_22_inspections.sql`
  2. `2026_04_22_notices.sql`
  3. `2026_04_22_renters_insurance.sql`
  4. `2026_04_22_leasing_assistant.sql`
  5. `2026_04_23_turnover_strategy.sql`
  6. `2026_04_23_move_out_info_notice.sql` *(adds enum value)*
  7. `2026_04_23_tenant_portal_token.sql`
  8. `2026_04_23_landlord_settings.sql`

### Env vars in use
Everything works with the current `.env.local`. New optional env vars for
future activation (all ship stubbed, no action required to run the app):

| Var | Feature that activates |
|---|---|
| `LEASING_ASSISTANT_ENABLED=true` + `ANTHROPIC_API_KEY` (or OpenAI / Retell) | Live AI drafts in Leasing assistant |
| `RETELL_API_KEY` | Real tenant-support + leasing phone lines |
| `RESEND_API_KEY` + verified domain | Landlord notification emails |
| Twilio SID + auth per landlord | Provisioned in the `/dashboard/settings/sms` UI, stored encrypted per landlord |
| `APPLICATION_ASSISTANT_ENABLED` (future) | Swap static FAQ for live LLM on `/apply/[slug]` |

---

## What's live (shippable as-is)

### Full modules (Units side)
- **Properties** · CRUD · CSV import (`/dashboard/properties/import`)
- **Listings** · public `/listings/[slug]` · syndication XML feed
- **Inspections** · move-in / move-out / periodic · photos · signatures · PDF export · move-in vs move-out comparison with damage summary
- **Maintenance** (existing; referenced by new action-items panel and reports)
- **Landlord insurance** (existing)

### Full modules (Tenants side)
- **Tenants** · CRUD · CSV import · token-based portal
- **Prospects** · existing + auto-created from public application form
- **Leasing Assistant** · AI conversation inbox with fair-housing guardrails · audit log + CSV export (stub-mode LLM)
- **Rent** · + CSV import for leases (`/dashboard/rent/import`)
- **Renewals** (existing)
- **Renters insurance tracker** (landlord-side) · required-but-missing alerts
- **Notices** · 6 types with state-rule-aware PDFs · mark-served workflow · audit trail

### Full modules (Operations)
- **Reports** — portfolio / rent collection / lease flow / maintenance / expenses (Schedule E) / YTD NOI
- **Settings → Voice + SMS lines** — two Retell-stubbed lines (support + leasing)

### Cross-cutting
- **Action items panel** on dashboard — 8 live signal queries
- **Workflows** — 6 guided flows (first-setup, onboard, offboard with lifecycle strip, annual-renewal, late-rent, turnover)
- **Tenant portal** — token-based read-only at `/portal/t/[token]`
- **Public application** — `/apply/[slug]` with AI FAQ helper
- **Syndication feed** — `/api/listings/feed/[token]/route.xml` in Zillow ILS format
- **Fair-housing audit log** — `/dashboard/leasing-assistant/audit` + CSV export
- **First-setup CTA** on empty dashboard for brand-new landlords

---

## What's stubbed (ready to activate)

| Feature | Stub location | Activation steps |
|---|---|---|
| Leasing assistant LLM | `app/lib/leasing/assistant-service.ts` | 1) `npm i @anthropic-ai/sdk` 2) Add `ANTHROPIC_API_KEY` + `LEASING_ASSISTANT_ENABLED=true` 3) Uncomment `liveLlmDraft` |
| Application assistant bot | `app/ui/application-faq-bot.tsx` | Same pattern — swap static FAQ lookup for live LLM call |
| Tenant support phone line | `app/lib/sms/retell-adapter.ts` | `docs/SPRINT-13-NEEDS.md` checklist — Retell account + Twilio creds + signature verification against live payload |
| Leasing (prospect) phone line | `app/actions/phone-lines.ts::provisionLeasingLine` | Same Retell account as support line. Webhook handler branches on `line_type` — still needs a prospect-specific code path to auto-create prospects instead of maintenance requests (one function to write) |
| Landlord notification emails | `app/lib/sms/resend-adapter.ts` + `resend-email-template.ts` | Templates are written (HTML + text). Wire `RESEND_API_KEY` + verified domain and swap the stub |

---

## What's blocked on external decisions

| Decision | Why blocked | What ships when unblocked |
|---|---|---|
| **LLM provider** | Pick Anthropic / OpenAI / Retell | Live AI drafts for leasing assistant + application helper |
| **Retell account + Twilio credentials** | Create accounts | Sprint 13 tenant-support line activates + prospect leasing line activates |
| **Resend account + verified sending domain** | DNS records + domain verification | Landlord email alerts send for real |
| **Rent-data vendor** (RentCast / HUD FMR / Zillow) | Vendor choice + API key | Rent pricing / comps widget on unit detail + renewal workflow |
| **Tenant renters insurance partner** (Lemonade / Sure / Roost) | Partnership + API | Sell policies through Rentbase vs. just tracking them |
| **Rent credit reporting** (Experian RentBureau / Piñata / CreditBoost) | Partner API | "Pay rent, build credit" tenant feature |
| **Tenant screening provider** (TransUnion SmartMove / Checkr) | Partner API | Auto-run credit/background on applications |
| **Stripe Connect onboarding** | Business entity setup for LLC + KYC | Real ACH + card rent collection |
| **Attorney review per state** | Legal review of notice templates | Remove "DRAFT — attorney review required" banner on notice PDFs |

---

## Git state (uncommitted)

97 files changed. Nothing has been committed.

**14 modified files (existing files extended):**
- `app/actions/phone-lines.ts` — added leasing-line provision + suspend
- `app/actions/photos.ts` — added inspection_items entity type
- `app/dashboard/layout.tsx` — reorganized sidebar into 3 groups + new entries
- `app/dashboard/listings/new/page.tsx` — accept `unitId` / `propertyId` query params
- `app/dashboard/listings/page.tsx` — mounted syndication feed card
- `app/dashboard/page.tsx` — action items panel + new stat cards + first-setup CTA + safe-wrap summaries
- `app/dashboard/properties/page.tsx` — "Import CSV" button
- `app/dashboard/prospects/new/page.tsx` — accept `unitId` query param
- `app/dashboard/rent/page.tsx` — "Import leases" button
- `app/dashboard/settings/sms/page.tsx` — two-card layout (support + leasing)
- `app/dashboard/tenants/[id]/page.tsx` — mounted tenant portal card
- `app/dashboard/tenants/page.tsx` — "Import CSV" button
- `app/lib/storage/photos.ts` — added `inspection_items` PhotoEntityType
- `app/listings/[slug]/page.tsx` — "Apply now" CTA

**83 new files** — see `git status` for full list. Grouped:
- Actions: inspections, lease-import, leasing, notices, property-import, public-application, renters-insurance, syndication, tenant-import, tenant-portal, turnover-strategy
- Queries: action-items, fair-housing-audit, inspection-compare, inspections, leasing, notices, public-listing, renters-insurance, reports, tenant-portal, workflow-context
- Schemas: inspection, leasing, notice, renters-insurance
- Pages (public): `/apply/[slug]`, `/portal/...`
- Pages (dashboard): inspections, leasing-assistant, notices, properties/import, rent/import, renters-insurance, reports, tenants/import, workflows
- API: `/api/listings/feed/[token]/route.ts`
- UI components: 30+
- Migrations: 8 new SQL files (all already applied to Supabase)
- CSV utility: `app/lib/csv.ts`
- Leasing: `app/lib/leasing/fair-housing-guardrails.ts` + `assistant-service.ts`
- SMS: `resend-email-template.ts`
- Inspection starter: `app/lib/inspection-templates.ts`

### Recommended commit plan

Don't squash — the work separates cleanly into logical chunks. Suggested sequence:

```
feat: move-in/out inspections with photos + signatures + PDF
feat: inspection move-in vs move-out comparison with damage summary
feat: state legal notice generator (6 types)
feat: renters insurance tracker (landlord-side)
feat: AI Leasing Assistant foundation with fair-housing guardrails
feat: fair-housing audit log + CSV export
feat: dashboard action items panel + summary widgets
feat: CSV imports (tenants, properties, leases)
feat: reorganize sidebar into Units / Tenants / Operations
feat: guided workflows (first-setup, onboard, offboard, renewal, late-rent, turnover)
feat: offboard → turnover → onboard lifecycle chain with turnover decision
feat: reports dashboard (portfolio / rent / leases / maintenance / expenses)
feat: tenant portal MVP (token-based read-only)
feat: public application form with AI FAQ helper
feat: listing syndication XML feed for Zillow / aggregators
feat: leasing phone line (parallel to Sprint 13 tenant support)
feat: Resend email templates for landlord alerts
```

Each commit should also pull in its dependent migration and any small helper files touched.

---

## Next-session priorities

### If Arthur picks Path A (stay indie-focused, polish + depth)
1. Security deposit itemized-accounting generator (auto-built from move-in/out comparison)
2. Late-fee auto-application cron (rent schedule → apply grace-period fee)
3. Prospect voice line webhook branch (finish the Retell handler so leasing calls auto-create prospects)
4. Recurring maintenance schedules (HVAC service, smoke detectors, annual inspections)
5. Rent increase calculator with state-cap validation

### If Arthur picks Path B (go up-market)
1. Multi-user / orgs / role-based permissions
2. White-label per-property branding
3. Yardi / Rent Manager integration layer
4. Owner/investor portal

### If Arthur picks Path C (activate external integrations)
1. Pick LLM provider + activate leasing assistant + application helper
2. Open Retell + Twilio accounts → activate Sprint 13 + leasing line
3. Pick rent-data vendor + wire it
4. Run notice templates past an attorney → remove DRAFT banners per state

---

## Known issues / things to watch

- **Service-role key in `.env.local`** — used by tenant portal, public application form, and syndication feed. Already scoped to narrow queries; watch for future features that might leak it wider.
- **Notice PDFs carry DRAFT banners** on every page — by design, until attorney review per state. Don't remove without legal sign-off.
- **A2P 10DLC registration** still deferred on the tenant support line — fine for testing volume, required before real tenant traffic (see `docs/SPRINT-13-NEEDS.md`).
- **The session log at `claude-obsidian/projects/rentapp/sessions/2026-04-22-session-start.md`** was started under the 2026-04-22 filename and spans three calendar days. Consider renaming `2026-04-22-to-04-24-build-sprint.md` on next touch.
- **Dev server timeout** — when I start `npm run dev` via the background shell tool, it dies after 2–10 minutes. Run it yourself in a local terminal for any real click-through work.

---

## Click-through demo script (for showing the work)

1. **Dashboard** — see action items panel, stat cards, first-setup CTA if empty
2. **⟶ Workflows** — browse 6 guided flows
3. **Start offboard workflow** on any active lease → see lifecycle strip (Offboard → Turnover → Onboard), turnover decision prompt, all 9 steps
4. **Inspections → start new move-in** → rate items → add photos → sign → Download PDF
5. **Notices → Generate → Rent increase** → fill form → Download PDF → Mark served
6. **✦ Leasing assistant → New conversation** → paste a prospect message with protected-class disclosures → see amber warnings → Generate AI draft → edit → approve
7. **Audit log → Export CSV**
8. **Tenant detail → Tenant portal access → Generate link → open in incognito tab**
9. **Public `/apply/[any-active-listing-slug]`** → try the AI helper with "what counts as income?"
10. **Listings → Syndication feed → Generate URL**, visit `/api/listings/feed/[token]/route.xml` → see ILS XML
11. **📊 Reports** → every panel
12. **Tenants → Import CSV** → Load sample → Import

---

## Files to read first if you're picking this up fresh

1. `docs/HANDOFF-2026-04-24.md` — this file
2. `CLAUDE.md` — project rules (fair-housing safe harbor, Next.js 16 gotchas, RLS discipline)
3. `AGENTS.md` — LLM-facing rules
4. `docs/SPRINT-13-NEEDS.md` — tenant SMS line activation
5. `db/migrations/2026_04_2[234]_*.sql` — everything added this session
6. Session log: `C:/Users/asmel/Desktop/coding with Claude/claude-obsidian/projects/rentapp/sessions/2026-04-22-session-start.md`

---

*Handoff generated by Claude Opus 4.7 at session close on 2026-04-24.*
